---
title: Example Partials
description: Three different ways to create partials 
---
<div class="page-header">
  <h1>{{title}}</h1>
  <p class="lead"></p> 
</div>
<section id="panels">
  <h2>Panels</h2>
  <div class="row">
    <div class="span3">

      {{! you can establish context inside the partials }}
      {{> panel panel.module-one}}
      {{> panel panel.module-two}}
      {{> panel panel.module-three}}
      {{> panel panel.module-four}}
      {{> panel panel.module-five}}
      {{> panel panel.module-six}}

    </div>
    <div class="span9">

      {{! or you can wrap the partials in block expressions to establish context }}
      {{#panel}}
        {{> panel module-seven}}
      {{/panel}}

      {{! or like this }}
      {{#panel.module-eight}}
        {{> panel }}
      {{/panel.module-eight}}

    </div>
  </div>
</section>

